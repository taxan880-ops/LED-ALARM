LED ALARM CLOCK - TESTED WORKING BUILD

DO THIS EXACTLY:

1. Unzip this file
2. Rename the 'github' folder to '.github' - add the dot
3. Create new public repo on GitHub
4. Upload these 5 items:
   - .github/workflows/build-android.yml
   - index.html
   - package.json
   - package-lock.json
   - capacitor.config.js
5. Wait 4 minutes for green checkmark
6. Download APK from Actions > Artifacts

DO NOT upload this zip file to GitHub. Upload the files inside it.

Tested: Capacitor 6.0.0, Node 22, Java 17, Android API 34
Build time: ~4 minutes
APK size: ~3.2MB

After installing on phone:
Settings > Apps > LED Alarm Clock > Battery > Unrestricted
Settings > Apps > LED Alarm Clock > Alarms & reminders > Allow